import React, {Component} from 'react';
import Counter from '../components/counter'
export default class Root extends Component {
  render() {
    return (
      const store = this.props.store 
      <Counter 
        value={}
        onIncrement={() => {

        }}
        onDecrement={this.props.store}
      ></Counter>
    )
  }
}